{
  "entities": {
    "Wallet": {
      "title": "Wallet",
      "description": "User's monthly budget wallet document",
      "type": "object",
      "properties": {
        "budget": {
          "type": "integer",
          "description": "Monthly budget in local currency"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Wallet creation timestamp"
        }
      },
      "required": ["budget", "createdAt"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "Individual income or expense transaction",
      "type": "object",
      "properties": {
        "walletId": {
          "type": "string",
          "description": "Owner user ID"
        },
        "description": {
          "type": "string",
          "description": "Brief note about the expense"
        },
        "amount": {
          "type": "integer",
          "description": "Value of the transaction"
        },
        "category": {
          "type": "string",
          "enum": ["comida", "transporte", "compras", "servicios", "entretenimiento", "otros"],
          "description": "Transaction category"
        },
        "day": {
          "type": "integer",
          "description": "Day of the month"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp"
        }
      },
      "required": ["walletId", "description", "amount", "category", "day", "createdAt"]
    },
    "RecurringExpense": {
      "title": "RecurringExpense",
      "description": "Monthly recurring fixed expense template",
      "type": "object",
      "properties": {
        "walletId": {
          "type": "string",
          "description": "Owner user ID"
        },
        "description": {
          "type": "string",
          "description": "Name or concept of the monthly expense"
        },
        "amount": {
          "type": "integer",
          "description": "Cost of the expense"
        },
        "category": {
          "type": "string",
          "enum": ["comida", "transporte", "compras", "servicios", "entretenimiento", "otros"],
          "description": "Expense category"
        },
        "dayOfMonth": {
          "type": "integer",
          "description": "Expected payment day of the month"
        },
        "isPaid": {
          "type": "boolean",
          "description": "Paid status for the current month"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp"
        }
      },
      "required": ["walletId", "description", "amount", "category", "dayOfMonth", "isPaid", "createdAt"]
    }
  },
  "firestore": {
    "/wallets/{walletId}": {
      "schema": "Wallet",
      "description": "Contains user wallets"
    },
    "/transactions/{transactionId}": {
      "schema": "Transaction",
      "description": "Contains all expenses and transactions for users"
    },
    "/recurring_expenses/{expenseId}": {
      "schema": "RecurringExpense",
      "description": "Contains monthly recurring fixed expenses"
    }
  }
}
